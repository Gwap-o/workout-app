# Netlify Configuration for Kinobody Workout Tracker

[build]
  # Build command
  command = "npm run build"

  # Publish directory (Vite outputs to dist)
  publish = "dist"

  # Environment variables (add these in Netlify Dashboard)
  # VITE_SUPABASE_URL
  # VITE_SUPABASE_PUBLISHABLE_KEY

# Redirect all requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

# Cache static assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Plugin: Netlify Identity (optional, if using)
# [[plugins]]
#   package = "@netlify/plugin-identity"
